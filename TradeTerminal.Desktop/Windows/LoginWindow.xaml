<Window x:Class="TradeTerminal.Desktop.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TradeTerminal.Desktop.Windows"
        mc:Ignorable="d"
        Title="Вход в систему" Height="340" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" 
        Background="White" FontFamily="Calibri">

    <Grid Margin="30,20,30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 0: Заголовок -->
            <RowDefinition Height="Auto"/>
            <!-- 1: Логин подпись -->
            <RowDefinition Height="Auto"/>
            <!-- 2: Логин инпут -->
            <RowDefinition Height="Auto"/>
            <!-- 3: Пароль подпись -->
            <RowDefinition Height="Auto"/>
            <!-- 4: Пароль инпут -->
            <RowDefinition Height="Auto"/>
            <!-- 5: Кнопки -->
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <!-- Внимание: Убедитесь, что PrimaryBrush объявлен в App.xaml, иначе замените на Background="#4A90E2" -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="8" Padding="20,12" Margin="0,0,0,15">
            <TextBlock Text="🔐 Вход в систему" FontSize="20" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Логин -->
        <TextBlock Grid.Row="1" Text="Логин" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="txtLogin" FontSize="14" Height="36" VerticalContentAlignment="Center" Margin="0,0,0,12"/>

        <!-- Пароль -->
        <TextBlock Grid.Row="3" Text="Пароль" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="4" x:Name="txtPassword" FontSize="14" Height="36" VerticalContentAlignment="Center" Margin="0,0,0,20"/>

        <!-- Кнопки -->
        <!-- Внимание: Убедитесь, что стили объявлены в App.xaml, иначе временно удалите Style="..." -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Войти" Click="BtnLogin_Click" Style="{StaticResource PrimaryButton}" Width="120" Height="38" Margin="0,0,15,0"/>
            <Button Content="Отмена" Click="BtnCancel_Click" Style="{StaticResource SecondaryButton}" Width="120" Height="38"/>
        </StackPanel>
    </Grid>
</Window>
